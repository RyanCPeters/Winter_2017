#pragma once
#include <vector>

using namespace std;

class Solution {
	struct Positions;
public:
	double findMedianSortedArrays(vector<int>& nums1, vector<int>& nums2) {
		Positions n1Pos, n2Pos;

		
	}

private:
	struct Poisitions { int leftLen = 0, rightLen = 0, totalLen = 0; };
};