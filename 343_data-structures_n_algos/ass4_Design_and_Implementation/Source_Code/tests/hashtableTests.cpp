#include "hashtable.h"

// tests:
// - hash algorithm collision must be handled appropriately
// - calling enroll() with an already existing keypair should switch to update()
// - enroll should work, so should get
// - calling update() on a missing key should switch to enroll()
// - getNumberOfEntries() must be accurate

int main(){
  // run tests here
  return 0;
}
